---
phase: 01-foundation
plan: 04
type: execute
wave: 3
depends_on:
  - 01-02
  - 01-03
files_modified:
  - app/layout.tsx
  - app/page.tsx
  - .gitignore
autonomous: false
requirements:
  - FOUND-02
  - FOUND-03
  - FOUND-04

must_haves:
  truths:
    - "Visiting the local dev server shows a dark page with a sticky nav at the top and a footer at the bottom"
    - "The nav bar remains visible when scrolling a long page"
    - "The page renders without horizontal scrollbar at 375px viewport width"
    - "The footer displays the GitHub link, MIT license badge, and NVIDIA attribution"
    - "A demo code block on the homepage renders with syntax highlighting and a copy button"
    - "pnpm build produces an out/ directory without errors"
  artifacts:
    - path: "app/layout.tsx"
      provides: "Root layout wrapping all pages with Nav and Footer"
      contains: "<Nav />"
    - path: "app/page.tsx"
      provides: "Homepage stub showing the design system and CodeBlock in action"
  key_links:
    - from: "app/layout.tsx"
      to: "components/nav/Nav.tsx"
      via: "import Nav from '@/components/nav/Nav'"
      pattern: "import Nav"
    - from: "app/layout.tsx"
      to: "components/footer/Footer.tsx"
      via: "import Footer from '@/components/footer/Footer'"
      pattern: "import Footer"
    - from: "app/page.tsx"
      to: "components/code/CodeBlock.tsx"
      via: "import CodeBlock from '@/components/code/CodeBlock'"
      pattern: "import CodeBlock"
---

<objective>
Wire the Nav, Footer, and CodeBlock components into the root layout and a placeholder homepage. This is the final plan of Phase 1 — it assembles everything into a deployable site skeleton that visually proves the design system, navigation, footer, and code block infrastructure all work together.

Purpose: After Plan 04, a human can visit the dev server and see the complete Phase 1 result — dark theme, sticky nav, footer, and a working code block demo — before Phase 2 builds the real landing page on top of it.
Output: A deployable skeleton site. `pnpm build` produces `out/`. Human verification confirms the visual result.
</objective>

<execution_context>
@/Users/dfejza/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dfejza/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
@.planning/phases/01-foundation/01-03-SUMMARY.md

<interfaces>
<!-- Components built in Plans 02 and 03 -->

From components/nav/Nav.tsx:
  export default function Nav(): JSX.Element
  // Renders sticky header with nav links and GitHubStars inside Suspense

From components/footer/Footer.tsx:
  export default function Footer(): JSX.Element
  // Renders footer with GitHub link, MIT badge, NVIDIA attribution

From components/code/CodeBlock.tsx:
  export default async function CodeBlock({
    code: string,
    lang?: string,         // defaults to 'bash'
    filename?: string,     // optional header label
  }): Promise<JSX.Element>
  // Async server component — must be used in a server context (layout, page, not client component)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Root layout with Nav and Footer</name>
  <files>app/layout.tsx</files>
  <action>
    Replace the auto-generated app/layout.tsx entirely with:

    ```typescript
    import type { Metadata } from 'next'
    import { Geist, Geist_Mono } from 'next/font/google'
    import Nav from '@/components/nav/Nav'
    import Footer from '@/components/footer/Footer'
    import './globals.css'

    const geist = Geist({
      variable: '--font-geist-sans',
      subsets: ['latin'],
    })

    const geistMono = Geist_Mono({
      variable: '--font-geist-mono',
      subsets: ['latin'],
    })

    export const metadata: Metadata = {
      title: 'MrHuang — Self-Hosted AI Companion',
      description:
        'Open-source, self-hosted AI companion with multiple personas. Your data stays on your hardware.',
    }

    export default function RootLayout({
      children,
    }: {
      children: React.ReactNode
    }) {
      return (
        <html lang="en" className={`${geist.variable} ${geistMono.variable}`}>
          <body className="flex min-h-dvh flex-col bg-canvas-default font-sans antialiased">
            <Nav />
            <main className="flex-1">{children}</main>
            <Footer />
          </body>
        </html>
      )
    }
    ```

    Notes:
    - `flex min-h-dvh flex-col` on body ensures Footer is always pushed to the bottom
    - `bg-canvas-default` from @theme applies the #0d1117 dark background globally
    - Geist font is included by default in create-next-app — no extra install needed
    - If Geist font import fails (unlikely but possible), fall back to: `import { Inter } from 'next/font/google'` and replace variable names
  </action>
  <verify>
    <automated>cd /Users/dfejza/Dev/project_mrhuangwebsite && grep -l "import Nav" app/layout.tsx && grep -l "import Footer" app/layout.tsx</automated>
  </verify>
  <done>
    - app/layout.tsx imports Nav and Footer from @/components/nav/Nav and @/components/footer/Footer
    - Body has flex min-h-dvh flex-col classes
    - metadata object has title and description
  </done>
</task>

<task type="auto">
  <name>Task 2: Homepage stub and production build verification</name>
  <files>app/page.tsx</files>
  <action>
    Replace app/page.tsx with a homepage stub that demonstrates the design system and code block component. This page will be replaced entirely in Phase 2 — it is a placeholder only.

    ```typescript
    import CodeBlock from '@/components/code/CodeBlock'

    const installCommand = `# Clone and run MrHuang
git clone https://github.com/dardanfejza/mrhuang
cd mrhuang
cp .env.example .env
docker compose up -d`

    export default async function HomePage() {
      return (
        <div className="mx-auto max-w-6xl px-4 py-24 sm:px-6 lg:px-8">
          {/* Placeholder heading */}
          <div className="mb-12 text-center">
            <p className="mb-3 text-sm font-medium uppercase tracking-widest text-nvidia">
              Phase 1 — Foundation
            </p>
            <h1 className="mb-4 text-4xl font-bold tracking-tight text-fg-default sm:text-5xl">
              Design System <span className="text-nvidia">Active</span>
            </h1>
            <p className="mx-auto max-w-xl text-lg text-fg-muted">
              Sticky nav, dark theme, NVIDIA green, responsive layout, and code blocks — all wired up.
              Phase 2 builds the real landing page here.
            </p>
          </div>

          {/* Color palette demo */}
          <div className="mb-12 grid grid-cols-2 gap-3 sm:grid-cols-4">
            {[
              { label: 'canvas-default', color: 'bg-canvas-default border border-border-default' },
              { label: 'canvas-subtle', color: 'bg-canvas-subtle' },
              { label: 'nvidia', color: 'bg-nvidia' },
              { label: 'border-default', color: 'border-2 border-border-default' },
            ].map(({ label, color }) => (
              <div key={label} className={`rounded-lg p-4 ${color}`}>
                <p className="text-xs font-mono text-fg-muted">{label}</p>
              </div>
            ))}
          </div>

          {/* Code block demo — exercises INST-03 */}
          <div className="mb-6">
            <h2 className="mb-4 text-lg font-semibold text-fg-default">
              Code Block Demo (INST-03)
            </h2>
            <CodeBlock
              code={installCommand}
              lang="bash"
              filename="Quick Install"
            />
          </div>

          {/* Responsive test indicator */}
          <p className="text-center text-xs text-fg-subtle">
            <span className="sm:hidden">Mobile (&lt;640px)</span>
            <span className="hidden sm:inline md:hidden">Tablet (sm)</span>
            <span className="hidden md:inline lg:hidden">Tablet (md)</span>
            <span className="hidden lg:inline xl:hidden">Desktop (lg)</span>
            <span className="hidden xl:inline">Wide (xl+)</span>
            {' '}breakpoint active — FOUND-02 responsive check
          </p>
        </div>
      )
    }
    ```

    After writing the file, run the production build:
    ```
    pnpm build
    ```

    The build MUST succeed and produce an `out/` directory. If it fails, diagnose and fix before proceeding.

    Common issues to check if build fails:
    - TypeScript errors in any component → fix type errors
    - shiki not resolving properly → ensure `pnpm add shiki` was run
    - Font import error → fall back to Inter font
    - `dangerouslySetInnerHTML` lint error → add biome-ignore comment (already in CodeBlock.tsx)
  </action>
  <verify>
    <automated>cd /Users/dfejza/Dev/project_mrhuangwebsite && pnpm build 2>&1 | tail -30</automated>
  </verify>
  <done>
    - pnpm build exits 0
    - out/ directory exists and contains index.html
    - No TypeScript errors
    - No missing module errors
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Phase 1 foundation: sticky nav with GitHub star CTA, dark GitHub-theme design system, footer with NVIDIA attribution, and code block with syntax highlighting and copy button. Wired into a placeholder homepage.
  </what-built>
  <how-to-verify>
    Run: `pnpm dev` — then open http://localhost:3000

    Check the following (each should PASS):

    1. **Dark theme**: Background is very dark (#0d1117). Text is light gray. NVIDIA green accent (#76b900) is visible.

    2. **Sticky nav**: Scroll down — nav bar remains fixed at top. Contains "MrHuang.ai" logo, Features / Install / Cost / GitHub links (hidden on mobile), and a star CTA button.

    3. **Responsive nav**: Resize browser to 375px width — nav links disappear, only logo and star button remain. No horizontal scrollbar appears.

    4. **Footer**: Scroll to bottom — shows GitHub link, MIT License badge, "Not affiliated with NVIDIA" text.

    5. **Code block**: A bash code block appears on the page with syntax highlighting (colored bash commands). Hover over it — a copy button appears in the top-right corner. Click copy — a green check appears briefly.

    6. **Responsive indicator**: Shows correct breakpoint label (Mobile / Tablet / Desktop) as you resize.

    7. **Build output**: Confirm `out/` directory exists and `out/index.html` is present.

    If any item FAILS, describe the issue and Claude will fix it before proceeding to Phase 2.
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe specific failures to fix.</resume-signal>
</task>

</tasks>

<verification>
After automated tasks complete (before checkpoint):
1. `grep "import Nav" app/layout.tsx` returns a match
2. `grep "import Footer" app/layout.tsx` returns a match
3. `grep "CodeBlock" app/page.tsx` returns a match
4. `pnpm build` exits 0
5. `ls out/index.html` — file exists

After checkpoint approval:
- Human confirms all 7 visual checks pass
</verification>

<success_criteria>
- Root layout wires Nav and Footer into every page
- Homepage stub demonstrates design system, responsive layout, and code block infrastructure
- `pnpm build` produces `out/` without errors — site is deployable to Cloudflare Pages
- Human has confirmed: dark theme, sticky nav, responsive collapse, footer NVIDIA attribution, code block highlighting + copy button all work visually
- Phase 1 foundation is complete — Phase 2 can build on top without config concerns
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-04-SUMMARY.md` with:
- Confirmation of visual checks (from checkpoint)
- Final file list for Phase 1
- Any issues encountered and resolved
- Build output summary (bundle size, page count)
- Notes for Phase 2 (anything the next phase executor needs to know about Phase 1 decisions)
</output>
