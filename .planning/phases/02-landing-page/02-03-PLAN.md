---
phase: 02-landing-page
plan: 03
type: execute
wave: 2
depends_on: [02-01, 02-02]
files_modified:
  - components/sections/ComingSoonSection.tsx
  - app/page.tsx
autonomous: false
requirements: [SOON-01]

must_haves:
  truths:
    - "Visiting the site root shows the complete landing page: hero, features, security, and coming soon sections scrolling in sequence"
    - "Coming Soon section teases Mac, iOS, Android native apps with a GitHub watch/star CTA link"
    - "The 'Star on GitHub' CTA in the hero shows a live star count (or graceful fallback) fetched at build time"
    - "pnpm build exits 0 with no TypeScript or build errors"
    - "All four section IDs/anchors work: Nav links scroll to correct sections"
  artifacts:
    - path: "components/sections/ComingSoonSection.tsx"
      provides: "Coming Soon teaser section — Mac/iOS/Android, GitHub CTA"
      exports: ["default ComingSoonSection"]
      contains: "github.com/dardanfejza/mrhuang"
    - path: "app/page.tsx"
      provides: "Root landing page — async Server Component composing all four sections"
      exports: ["default HomePage"]
      contains: "getStarCount"
  key_links:
    - from: "app/page.tsx"
      to: "lib/github.ts"
      via: "await getStarCount() at build time, result passed as stars prop to HeroSection"
      pattern: "getStarCount"
    - from: "app/page.tsx"
      to: "components/sections/HeroSection.tsx"
      via: "stars prop"
      pattern: "<HeroSection stars="
    - from: "app/page.tsx"
      to: "components/sections/FeaturesSection.tsx"
      via: "direct import, no props"
      pattern: "<FeaturesSection"
    - from: "app/page.tsx"
      to: "components/sections/SecuritySection.tsx"
      via: "direct import, no props"
      pattern: "<SecuritySection"
    - from: "app/page.tsx"
      to: "components/sections/ComingSoonSection.tsx"
      via: "direct import, no props"
      pattern: "<ComingSoonSection"
---

<objective>
Complete the landing page by building the ComingSoonSection and wiring all four sections into app/page.tsx.

Purpose: ComingSoonSection creates excitement for native apps with a minimal static teaser. Page assembly replaces the Phase 1 placeholder stub with the real landing page, plumbing the build-time star count from lib/github.ts into HeroSection.
Output: `components/sections/ComingSoonSection.tsx` (new) and `app/page.tsx` (replaced). Build must pass.
</objective>

<execution_context>
@/Users/dfejza/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dfejza/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-landing-page/02-CONTEXT.md
@.planning/phases/02-landing-page/02-RESEARCH.md
@.planning/phases/02-landing-page/02-01-SUMMARY.md
@.planning/phases/02-landing-page/02-02-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From lib/github.ts (do NOT re-implement):
```typescript
export async function getStarCount(): Promise<number>
// Fetches GitHub star count at build time. Falls back to 0 if API unreachable.
```

From components/sections/HeroSection.tsx (created in Plan 01):
```typescript
// Props interface:
interface HeroSectionProps {
  stars?: number
}
export default function HeroSection({ stars }: HeroSectionProps): JSX.Element
```

From components/sections/FeaturesSection.tsx (created in Plan 02):
```typescript
export default function FeaturesSection(): JSX.Element
// No props — self-contained
```

From components/sections/SecuritySection.tsx (created in Plan 02):
```typescript
export default function SecuritySection(): JSX.Element
// No props — self-contained
```

Lucide icons for ComingSoonSection (confirmed in lucide-react 0.575.0):
```typescript
import { Monitor, Smartphone, Github } from 'lucide-react'
// Monitor = Mac/desktop, Smartphone = iOS and Android (no separate icons in lucide-react)
```

Established layout wrapper:
```
<div className="mx-auto max-w-6xl px-4 sm:px-6 lg:px-8">
```

Design tokens — same as prior sections (see 02-RESEARCH.md for full list).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ComingSoonSection for native apps teaser</name>
  <files>components/sections/ComingSoonSection.tsx</files>
  <action>
Create `components/sections/ComingSoonSection.tsx` as a plain Server Component — NO `"use client"` directive. NO email capture form (locked decision — GitHub link only).

**Structure:**
```tsx
<section className="py-16 sm:py-20">
  <div className="mx-auto max-w-6xl px-4 sm:px-6 lg:px-8 text-center">
    {/* Heading + sub-copy */}
    {/* Three platform teasers */}
    {/* GitHub watch CTA */}
  </div>
</section>
```

**Heading (self-aware humorous tone — HERO-04):**
- h2: "Coming soon. (MrHuang promised.)" or "What's next. Allegedly."
- Sub-copy: e.g., "Native apps for the platforms you actually use. Mac, iOS, Android — because the browser isn't always where you need your AI companion."

**Three platform teasers (SOON-01):**
Use Monitor for Mac, Smartphone for both iOS and Android (lucide-react has no separate iOS/Android icons):
```typescript
const platforms = [
  { icon: Monitor,    label: 'Mac',     desc: 'Native menu bar app. Always there when you need it.' },
  { icon: Smartphone, label: 'iOS',     desc: 'iPhone and iPad. MrHuang in your pocket.' },
  { icon: Smartphone, label: 'Android', desc: 'Android native. Because choice matters.' },
]
```

Display as a centered flex row or 3-col grid of small cards. Keep these minimal — they're teasers, not feature showcases:
```tsx
<div className="mt-8 flex flex-col items-center gap-4 sm:flex-row sm:justify-center">
  {platforms.map(({ icon: Icon, label, desc }) => (
    <div key={label} className="flex flex-col items-center rounded-xl border border-border-default bg-canvas-subtle p-6 text-center w-full sm:w-48">
      <Icon className="mb-2 h-6 w-6 text-nvidia" />
      <p className="text-sm font-semibold text-fg-default">{label}</p>
      <p className="mt-1 text-xs text-fg-muted">{desc}</p>
    </div>
  ))}
</div>
```

**GitHub watch CTA (SOON-01):**
A single anchor — NOT a form, NOT an email input:
```tsx
<a
  href="https://github.com/dardanfejza/mrhuang"
  target="_blank"
  rel="noopener noreferrer"
  className="mt-8 inline-flex items-center gap-2 rounded-lg border border-border-default bg-canvas-subtle px-5 py-3 text-sm font-medium text-fg-default transition-colors hover:border-nvidia/50 hover:text-nvidia"
>
  <Github className="h-4 w-4" />
  Watch on GitHub for updates
</a>
```

**Imports:**
```typescript
import { Monitor, Smartphone, Github } from 'lucide-react'
```

**Rules:**
- No `"use client"` directive
- No email capture or form elements (locked decision)
- Keep it minimal and honest — it's a teaser, not a commitment
  </action>
  <verify>
    <automated>cd /Users/dfejza/Dev/project_mrhuangwebsite && pnpm tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - `components/sections/ComingSoonSection.tsx` exists
    - Three platform cards (Mac, iOS, Android) with icons and descriptions
    - GitHub watch CTA link present (no form, no email input)
    - No `"use client"` directive
    - TypeScript compiles clean
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire app/page.tsx and verify production build</name>
  <files>app/page.tsx</files>
  <action>
Replace the entire content of `app/page.tsx` (the Phase 1 placeholder stub) with the real landing page.

**IMPORTANT:** Read the current `app/page.tsx` first to confirm it is the Phase 1 stub (contains design system demo, CodeBlock demo, breakpoint indicator). Then replace it completely.

Also check if `app/page.module.css` exists — if it does, delete it (it was from the original scaffold, no longer needed).

**New app/page.tsx:**
```typescript
// app/page.tsx — async Server Component (no "use client")
// Phase 2: Real landing page replacing Phase 1 placeholder stub
import HeroSection from '@/components/sections/HeroSection'
import FeaturesSection from '@/components/sections/FeaturesSection'
import SecuritySection from '@/components/sections/SecuritySection'
import ComingSoonSection from '@/components/sections/ComingSoonSection'
import { getStarCount } from '@/lib/github'

export default async function HomePage() {
  const stars = await getStarCount()  // build-time fetch, falls back to 0

  return (
    <div>
      <HeroSection stars={stars} />
      <FeaturesSection />
      <SecuritySection />
      <ComingSoonSection />
    </div>
  )
}
```

The function MUST be `async` to support `await getStarCount()`. No `"use client"` directive.

**After writing the file, run the production build:**
```bash
cd /Users/dfejza/Dev/project_mrhuangwebsite && pnpm build
```

The build must exit 0. If it fails:
1. Check TypeScript errors first: `pnpm tsc --noEmit`
2. Fix any import path issues (use `@/` aliases not relative paths)
3. Fix any missing exports from section components
4. Re-run `pnpm build` after each fix

**Do NOT touch:**
- `app/layout.tsx` — root layout is stable from Phase 1
- `components/nav/` — Phase 1, do not modify
- `components/footer/` — Phase 1, do not modify
- `lib/github.ts` — do not re-implement
  </action>
  <verify>
    <automated>cd /Users/dfejza/Dev/project_mrhuangwebsite && pnpm build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - `app/page.tsx` is async, imports all four section components, calls `getStarCount()`, passes `stars` to HeroSection
    - `pnpm build` exits 0 with no errors
    - No `"use client"` in page.tsx
    - `app/page.module.css` removed if it existed (or confirmed absent)
    - Build output shows `/` route as static HTML
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of complete landing page</name>
  <files>app/page.tsx, components/sections/HeroSection.tsx, components/sections/FeaturesSection.tsx, components/sections/SecuritySection.tsx, components/sections/ComingSoonSection.tsx</files>
  <action>Human visual verification of the complete landing page. Run `pnpm dev` then check all sections at http://localhost:3000 against the checklist below.</action>
  <verify>
    <automated>pnpm build 2>&1 | tail -5</automated>
  </verify>
  <done>All seven checklist items confirmed: hero with persona cards and disclaimers, features with /#features anchor, security with CSS diagram and 5 cards, coming soon with platform teasers, no mobile overflow, correct contrast on green backgrounds.</done>
  <what-built>Complete mrhuang.ai landing page — four sections assembled and built as a static site. Run `pnpm dev` or open `out/index.html` to verify.</what-built>
  <how-to-verify>
    Start the dev server: `cd /Users/dfejza/Dev/project_mrhuangwebsite && pnpm dev`, then open http://localhost:3000

    Check these items in order:

    1. **Hero section** — "Meet MrHuang" headline visible, humorous sub-copy present, three persona cards (Mentor/Therapist/Life Coach) with initials placeholders, "Star on GitHub" green CTA button, "100% Open Source" badge/link
    2. **Persona card disclaimers** — each of the three cards shows "AI-generated, not affiliated with NVIDIA or Jensen Huang" text beneath the tagline
    3. **Features section** — Nav "Features" link scrolls correctly to features section (not top of page), six feature cards in grid, "Your data stays on your hardware" shield callout visible
    4. **Security section** — data flow diagram shows four nodes (Browser → Cloudflare Tunnel → Home Server → Claude API), Home Server node has green ring/highlight, five security cards below it, "Your data never leaves your hardware" message visible
    5. **Coming Soon section** — Mac/iOS/Android cards with icons, "Watch on GitHub" link present, no email form
    6. **Mobile (375px)** — resize to 375px width: security diagram stacks vertically (no horizontal overflow), feature cards are 2-column, persona cards stack to single column, no horizontal scrollbar anywhere
    7. **Green text contrast** — any text on NVIDIA green backgrounds uses dark text (not white)
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
1. `pnpm tsc --noEmit` — no TypeScript errors
2. `pnpm build` — exits 0, generates `out/` directory
3. `pnpm dev` + browser check at http://localhost:3000 — all four sections visible and functional
4. Mobile viewport check at 375px — no horizontal overflow
</verification>

<success_criteria>
Complete landing page live: all four sections (hero, features, security, coming soon) scroll correctly, Nav anchors work, build passes, human visual verification approved. All 12 Phase 2 requirements covered across the three plans.
</success_criteria>

<output>
After completion, create `.planning/phases/02-landing-page/02-03-SUMMARY.md`
</output>
